#include "main.h"

/**
 * _strlen - returns the lenght of a string
 * @s: string
 * Return: lenght
 */

int _strlen(char *s)
{
	int longi = o;

	while (*s != '\0')
	{
		long1++;
		s++;
	}
	return (longi);
}
